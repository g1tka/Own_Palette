<p>新規登録ページ</p>
<div class="container">
  <div class="row justify-content-center">
    <%= form_with model: @user, url: user_registration_path, local: true do |f| %>
      <table class="user_form">
        <tr>
          <th><%= f.label :name, "User name" %></th>
          <td><%= f.text_field :name, autofocus: true, :placeholder => "  to 20 characters" %></td>
        </tr>
        <tr>
          <th><%= f.label :email, "Email" %></th>
          <td><%= f.text_field :email %></td>
        </tr>
        <tr>
          <th><%= f.label :password, "Password" %></th>
          <td><%= f.password_field :password, autocomplete: "new-password", :placeholder => " minimum 6 characters" %></td>
        </tr>
        <tr>
          <th><%= f.label :password_confirmation, "Password Confirmation"  %></th>
          <td><%= f.password_field :password_confirmation, autocomplete: "new-password" %></td>
        </tr>
      </table>
      <div class="col mt-3">
        <%= f.submit "Sign in", class: "btn btn-success" %>
      </div>
    <% end %>
    <% if @user.errors.any? %>
      <div class="col mt-3 mx-5">
        <%= render 'layouts/error_messages', resource: @user %>
      </div>
    <% end %>
  </div>
</div>
<div class="row mt-5 d-flex justify-content-center">
  既にアカウントをお持ちの方は
  <%= link_to 'こちら', new_user_session_path %>
</div>