<% if current_user.blocking?(user) %>
  <%= link_to "ブロック解除", user_destroy_block_path(user.id), method: :delete, remote: true, class: "btn btn-danger" %>
<% else %>
  <%= link_to "ブロック", user_create_block_path(user.id), method: :post, remote: true, class: "btn btn-outline-danger" %>
<% end %>