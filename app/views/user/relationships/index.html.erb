<%= @user.name %>さんのフォロー/フォロワー一覧ページ
<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-4 col-md-8 col-xs-12 my-2 py-3 px-4 mx-2 follow-box">
      <div class="border-bottom">
        フォロワー一覧
      </div>
      <% if @user.followers.exists? %>
        <% @user.followers.each do |follower| %>
          <div class="mt-3">
            <%= link_to user_path(follower) do %>
              <%= follower.name %>
            <% end %>
          </div>
        <% end %>
      <% else %>
        <div class="mt-3">
          フォロワーはいません。
        </div>
      <% end %>
    </div>
    <div class="col-lg-4 col-md-8 col-xs-12 my-2 py-3 px-4 mx-2 follow-box">
      <div class="border-bottom">
        フォロー一覧
      </div>
      <% if @user.followings.exists? %>
        <% @user.followings.each do |following| %>
          <div class="mt-3">
            <%= link_to user_path(following) do %>
              <%= following.name %>
            <% end %>
          </div>
        <% end %>
      <% else %>
        <div class="mt-3">
          フォローしているユーザーはいません。
        </div>
      <% end %>
    </div>
    <div class="col-lg-3 col-md-8 col-xs-12 my-2 py-3 px-4 mx-2 block-box">
      <div class="border-bottom">
        ブロック一覧
      </div>
      <% if @user.blockings.exists? %>
        <% @user.blockings.each do |blocking| %>
          <div class="mt-3">
            <%= link_to user_path(blocking) do %>
              <%= blocking.name %>
            <% end %>
          </div>
        <% end %>
      <% else %>
        <div class="mt-3">
          ブロックしているユーザーはいません。
        </div>
      <% end %>
    </div>
  </div>
</div>