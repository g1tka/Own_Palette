<p>ログインページ</p>
<div class="container">
  <div>
    <!--"退会済のアカウントです。新規登録をお願いします。"from sessions controller-->
    <%= flash[:danger] %>
  </div>
  <div class="row justify-content-center">
    <%= form_with model: @user, url: user_session_path, local: true do |f| %>
      <table class="user_form">
        <tr>
          <!--emailとPWでログインできるという要件のため名前でのログインは実装しない-->
          <th><%#= f.label :name, "User name" %></th>
          <td><%#= f.text_field :name, autofocus: true %></td>
        </tr>
        <tr>
          <th><%= f.label :email %></th>
          <td><%= f.text_field :email, autofocus: true %></td>
        </tr>
        <tr>
          <th><%= f.label :password %></th>
          <td><%= f.password_field :password, autocomplete: "new-password" %></td>
        </tr>
      </table>
      <div class="col mt-3">
        <%= f.submit "Log in", class: "btn btn-success" %>
      </div>
    <% end %>
    <p class="alert"><%= alert %></p>
  </div>
      </div>
  <div class="row justify-content-center mt-3">
    <div class="col mr-4">
      <%= link_to "ゲストログイン（閲覧用）",users_guest_sign_in_path, class: "btn btn-sm btn-info mb-3 sign_in", method: :post %>
    </div>
  </div>

  <div class="row mt-5 d-flex justify-content-center">
    まだアカウントをお持ちでない方は
    <%= link_to 'こちら', new_user_registration_path %>
  </div>
</div>