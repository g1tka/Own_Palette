ユーザーページ(<%= @user.name %>さん)
<div class="container">
  <div class="row justify-content-start ml-3 mb-4 mt-3">
    <%= link_to user_path(current_user, filter: "owm"), class: "btn btn-sm btn-outline-secondary mx-2" do %>
      <i class="fa-solid fa-image-portrait"></i>
      自分の投稿
    <% end %>
    <%= link_to user_path(current_user, filter: "favorites"), class: "btn btn-sm btn-outline-secondary mx-2" do %>
      <i class="fa-solid fa-hand-holding-heart"></i>
      いいねした投稿
    <% end %>
    <%= link_to user_path(current_user, filter: "all"), class: "btn btn-sm btn-outline-secondary mx-2" do %>
      <i class="fa-regular fa-images"></i>
      すべてのコレクション
    <% end %>
  </div>
  <div class="row">
    <% if @posts.any? %>
      <% @posts.each do |post| %>
        <% if post.is_open || post.user == current_user %>
          <div class="col-md-4 mb-4">
            <%= link_to post_path(post) do %>
              <div class="flame ml-lg-5 text-center mb-4 mb-md-0">
                <div class="image m-3">
                  <%= image_tag post.image, size: "200x200" %>
                </div>
                <div class="row d-flex justify-content-end mr-1">
                  <div classs="badge bg-primary text-wrap", style="width: 12em;">
                    <%= link_to user_path(post.user) do %>
                      <%= post.user.name %>
                    <% end %>
                  </div>
                  <div class="mr-2 mt-1">
                    <% if post.favorited_by?(current_user) %>
                      <%= link_to post_favorites_path(post), method: :delete do %>
                        <i class="fa-solid fa-heart", style="color: red;"></i>
                        <%= post.favorites.count %> いいね&ensp;
                      <% end %>
                    <% else %>
                      <%= link_to post_favorites_path(post), method: :post do %>
                        <i class="fa-regular fa-heart"></i>
                        <%= post.favorites.count %> いいね&ensp;
                      <% end %>
                    <% end %>
                      <i class="fa-regular fa-comment"></i>
                      <%= post.comments.count %> コメント
                  </div>
                </div>
              </div>
              <div class="text-center m-3">
                <%= post.body %>
              </div>
            <% end %>
          </div>
        <% end %>
      <% end %>
    <% else %>
      <div class="col my-5">
        まだ投稿はありません。
      </div>
    <% end %>
  </div>

  
  <% if @user == current_user %>
    <div class="row mt-3 ml-3">
      <%= link_to 'Edit', edit_user_registration_path %>
      &nbsp;profile?
    </div>
  <% end %>
  
</div>